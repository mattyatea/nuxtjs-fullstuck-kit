generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
  output          = "../server/prisma-client"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}
